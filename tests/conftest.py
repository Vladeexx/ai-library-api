# Test fixtures will be added in a future phase.
#
# Planned:
#   - async test engine using SQLite (aiosqlite) or a dedicated test PostgreSQL DB
#   - AsyncClient fixture via httpx for integration tests
#   - Redis mock via fakeredis
#   - Book factory fixture for seeding test data
